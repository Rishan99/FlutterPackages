name: charts_flutter
version: 0.12.0
description: Material Design charting library for flutter.
homepage: https://github.com/google/charts

environment:
  sdk: ">=3.8.0 <4.0.0"
  flutter: ">=3.38.0"

dependencies:
  charts_common:
    path: ../charts_common/
  flutter:
    sdk: flutter
  intl: ^0.20.2
  logging: ^1.3.0

dev_dependencies:
  build_runner: ^2.15.0
  flutter_test:
    sdk: flutter
  mockito: ^5.0.0
  test: ^1.5.3
